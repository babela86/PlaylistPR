<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>dei.uc.pt.ar.paj</groupId>
		<artifactId>theparent</artifactId>
		<version>0.0.1-SNAPSHOT</version>
	</parent>

	<artifactId>anear-ear</artifactId>
	<packaging>ear</packaging>

	<!-- Adicionar os modulos que criámos -->
	<dependencies>
		<dependency>
			<groupId>dei.uc.pt.ar.paj</groupId>
			<artifactId>aweb-web</artifactId>
			<version>${project.version}</version>
			<type>war</type>
		</dependency>
		<dependency>
			<groupId>dei.uc.pt.ar.paj</groupId>
			<artifactId>thews-ws</artifactId>
			<version>${project.version}</version>
			<type>war</type>
		</dependency>
		<dependency>
			<groupId>dei.uc.pt.ar.paj</groupId>
			<artifactId>aservice-services</artifactId>
			<version>${project.version}</version>
			<type>ejb</type>
		</dependency>
		<dependency>
			<groupId>dei.uc.pt.ar.paj</groupId>
			<artifactId>lyric-ws-client</artifactId>
			<version>${project.version}</version>
			<type>ejb</type>
		</dependency>
	</dependencies>

	<build>
		<finalName>firstear</finalName>
		<plugins>
			<!--Ear plugin -creating the ear - watch out skinny WARS! -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-ear-plugin</artifactId>
				<configuration>
					<finalName>firstear</finalName>
					<defaultJavaBundleDir>lib/</defaultJavaBundleDir>
					<skinnyWars>true</skinnyWars>
					<modules>
						<webModule>
							<groupId>dei.uc.pt.ar.paj</groupId>
							<artifactId>aweb-web</artifactId>
						</webModule>
						<webModule>
							<groupId>dei.uc.pt.ar.paj</groupId>
							<artifactId>thews-ws</artifactId>
						</webModule>
						<ejbModule>
							<groupId>dei.uc.pt.ar.paj</groupId>
							<artifactId>aservice-services</artifactId>
						</ejbModule>
						<ejbModule>
							<groupId>dei.uc.pt.ar.paj</groupId>
							<artifactId>lyric-ws-client</artifactId>
						</ejbModule>
					</modules>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.wildfly.plugins</groupId>
				<artifactId>wildfly-maven-plugin</artifactId>
				<configuration>
				<hostname>localhost</hostname>
						<port>${server.port}</port>
					<skip>false</skip>
					<filename>firstear.ear</filename>
				</configuration>
			</plugin>
		</plugins>
	</build>
</project>